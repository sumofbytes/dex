[package]
name = "dex-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared wire protocol types for dex clients and daemons"
license = "MIT OR Apache-2.0"
repository = "https://github.com/sumofbytes/dex"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
