---
name: Bug report
about: Something in dex is broken
labels: bug
---

**What happened**

A clear and concise description of the bug.

**How to reproduce**

Steps or a minimal prompt/command that triggers it.

**Expected behavior**

What you expected to happen instead.

**Actual behavior**

Relevant transcript / terminal output.

**Environment**

- `dex --version`:
- OS and terminal:
- Provider + model (from `~/.config/dex/config.yaml` — redact API keys):
- How you run dex: TUI / one-shot / daemon + connect
- Permission mode, if relevant (`read-only` / `ask` / `trusted`; or the TUI agent mode `plan` / `manual` / `auto`)

**Additional context**

Session JSONL path or config snippet if relevant (redact secrets).
